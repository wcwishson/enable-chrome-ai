[project]
name = "enable-chrome-ai"
version = "1.2.0"
description = "One-click Windows app and Python helper for restoring Chrome's built-in Gemini and AI features."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.1.3",
]
